curl:
  if: vitality @/curl/curl

php:
  if: vitality @/php/php

composer:
  if:   which composer.phar
  else: sudo curl -sS https://getcomposer.org/installer | sudo php -- --install-dir=/usr/local/bin/
